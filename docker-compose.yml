version: '3.8'

services:
  web:
    image: notes-app-image:latest # Local image name
    container_name: notes-container
    ports:
      - "8000:8000"
    volumes:
      - /home/ubuntu/notes_data:/app/db_data
    # Ensure the container doesn't exit immediately if errors occur
    restart: always
